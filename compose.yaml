services:
  sharp:
    build:
      context: "./"
      dockerfile: "./Dockerfile"
    ports:
      - "${PORT-8080}:${PORT-8080}"
    environment:
      HOST: "${HOST-0.0.0.0}"
      PORT: "${PORT-8080}"
    volumes:
      - "./sharp.pt:/app/sharp.pt:ro"
